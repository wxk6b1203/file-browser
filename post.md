# 一个面向多种远程存储的桌面文件管理器：File Browser

如果你经常需要在本地磁盘、SFTP 服务器、S3 存储、阿里云 OSS 之间来回切换文件，你大概率遇到过同一种问题：工具很多，但体验往往是割裂的。

有的工具只适合传文件，不适合日常浏览；有的工具支持某一种协议，却不支持另一种；有的界面过于偏技术人员，不适合普通用户长期使用。这个项目想解决的，就是“用一套统一界面，管理多种文件来源”的问题。

## 这是什么？

`File Browser` 是一个桌面文件管理器。

它的目标不是单纯做一个“上传下载工具”，而是尽可能像一个日常可用的文件工作台：

- 可以管理本地目录
- 可以连接 SFTP 服务器
- 可以连接 S3 对象存储
- 可以连接阿里云 OSS
- 可以在不同连接之间拖拽文件和目录
- 可以像使用本地文件管理器一样，浏览、打开、删除、重命名、搜索和传输文件

从使用体验上，它整体参考了 VS Code 的交互方式：

- 左侧是连接和目录树
- 中间是文件工作区
- 支持多标签和分栏
- 支持任务面板和通知面板
- 支持键盘快捷键

但它的目标用户并不只是开发者。只要你需要在几种不同的文件系统之间来回工作，这个工具都可以降低很多重复操作成本。

## 它解决了什么问题？

最直接的价值有三个。

### 1. 把“多种存储”放进一个界面里

很多人的工作环境并不只有一个文件位置。

你可能会同时面对：

- 自己电脑上的本地文件
- 一台 Linux 服务器上的 SFTP 目录
- 一个 S3 Bucket
- 一个 OSS Bucket

以前你可能需要开好几个工具，甚至需要记住不同工具的不同操作方式。这个项目希望把这些入口统一起来，让它们在界面和交互上尽可能一致。

### 2. 把“传输”变成自然操作，而不是额外流程

很多工具把上传下载做成一个独立流程：先选文件，再选目标，再确认。

这个项目更强调“像文件管理器一样工作”：

- 可以拖拽上传
- 可以跨连接拖拽传输
- 可以直接双击打开文件
- 可以设置默认下载目录
- 可以从任务面板查看进度和结果

这让远程文件操作更接近日常使用本地文件管理器的体验。

### 3. 让桌面端文件管理更适合长期使用

项目在交互层做了不少细节打磨，例如：

- 列表视图和图标视图切换
- 列显示控制和排序
- 多选、快捷键、前进后退历史
- 局部搜索和全局搜索
- 内联新建目录、内联删除确认
- 目录树与工作区联动
- 跨平台滚动条与拖拽兼容修正

这些都不是“能不能用”的问题，而是“能不能持续用”的问题。

## 它适合谁？

这类工具适合下面几种用户：

- 运维人员：需要频繁访问服务器目录
- 开发者：需要在本地和对象存储之间来回处理文件
- 内容管理人员：需要上传、整理、核对远程文件
- 小团队内部工具使用者：需要统一的文件工作台，而不是命令行和多个后台页面
- 对命令行不熟悉，但又有远程文件管理需求的普通用户

如果你只是偶尔上传一个文件，浏览器后台页面也许够用；但如果你每天都要在多个位置来回处理文件，这种桌面工具的效率优势会越来越明显。

## 这个项目目前有哪些特点？

从当前实现来看，它已经具备比较完整的基础能力：

- 支持 Local / SFTP / S3 / OSS 多种连接
- 支持文件列表视图与图标视图
- 支持连接间拖拽传输
- 支持目录树和中央工作区联动
- 支持任务通知与传输进度
- 支持默认下载目录
- 支持文件外部打开
- 支持局部搜索和全局搜索
- 支持中、英、日三种界面语言
- 支持 Windows / macOS / Linux 的桌面构建

另外，项目在一些“看起来不起眼，但真实使用非常重要”的地方也做了不少处理，例如：

- 路径规范化和跨平台兼容
- 空目录行为一致性
- 多面板拖拽状态收口
- WebKit 和 WebView2 的差异兼容
- 文件与目录修改时间保留
- 对象存储目录语义的补齐

这些工作不会直接变成宣传点，但它们决定了这个工具到底是“演示能跑”，还是“真的能用”。

## 一个很特别的事实：这个项目 95% 以上的代码由 AI Agent 完成

这是这个项目最特别，也最值得记录的一点。

这个项目中，**95% 以上的代码由 Codex、Claude 等 AI Agent 直接参与完成**，包括但不限于：

- 功能设计与实现
- 前后端联调
- 拖拽与交互修复
- 配置与路径兼容处理
- 目录树与文件面板联动
- 测试补充与回归修复
- 文档整理与 CI 工作流编写

这不意味着“人完全不参与”。恰恰相反，真正重要的是：

- 人来定义方向
- 人来判断产品取舍
- 人来发现真实使用中的问题
- Agent 来承担大量具体实现、排查、测试和重构工作

从这个角度看，这个项目本身也是一次很直接的实践：AI Agent 已经不只是写几个示例函数，而是可以在一个真实桌面项目里，持续承担大部分工程实现工作。

当然，这也带来了新的要求：

- 要有清晰的架构边界
- 要持续 review 改动
- 要补测试，防止回归
- 要知道哪些能力是前端能做的，哪些必须下沉到原生层

所以，这不是“把代码交给 AI 就结束了”，而是“人和 Agent 形成一种新的协作方式”。这个项目正是这种协作方式的一个具体样本。

## 为什么这件事值得被记录？

因为它说明了一件越来越现实的事：

在一个中等复杂度、涉及前端、后端、桌面框架、对象存储、拖拽交互、跨平台兼容的项目里，AI Agent 已经可以承担主力工程实现。

这不代表软件开发会变成“按一下按钮就自动完成”。

但它确实意味着，未来很多软件项目的开发方式会发生变化：

- 需求描述会更重要
- 架构判断会更重要
- review 和验证会更重要
- 人类工程师会更像“系统设计者 + 审核者 + 决策者”

而具体的大量实现，越来越可能由 Agent 来完成。

## 接下来可以期待什么？

这个项目后续仍然有不少可以继续完善的方向，例如：

- 更成熟的批量文件操作
- 更完整的搜索能力
- 更丰富的对象存储元数据支持
- 更强的文件预览和编辑能力
- 更进一步的多平台原生拖拽能力
- 更完整的发布和签名流程

如果这些能力继续补齐，它会越来越接近一个真正适合长期使用的跨存储文件管理工具。

## 结语

`File Browser` 的价值，不只是“它能连 SFTP、S3 和 OSS”。

更重要的是，它尝试用统一的桌面交互，把原本分散、割裂、偏技术化的文件操作体验，整理成一个普通用户也能逐步上手的工具。

同时，它也展示了另一件同样重要的事：在真实项目里，AI Agent 已经可以承担绝大多数工程实现工作。这个项目超过 95% 的代码由 Codex、Claude 等 Agent 完成，本身就是一个很有代表性的信号。

如果你既关心文件管理工具的实际体验，也关心 AI Agent 如何真正进入软件生产流程，那么这个项目值得持续关注。
