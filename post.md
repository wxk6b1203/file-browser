# 我做了一个桌面文件管理器：能连本地、SFTP、S3、OSS，而且 95% 以上代码由 AI Agent 完成

如果你经常在这些地方来回找文件：

- 自己电脑里的本地目录
- Linux 服务器上的 SFTP
- AWS S3
- 阿里云 OSS

那你大概率对一种体验很熟：

工具不少，但总觉得不顺手。

有的工具只适合“传文件”，不适合持续浏览和整理；有的工具只能连一种存储；有的后台页面能凑合用，但操作总像是在完成任务，而不是在真正“管理文件”。

我做这个项目，最初就是因为这个问题。

我想要的不是另一个“上传下载工具”，而是一个**真的能拿来当日常工作台用的桌面文件管理器**。

它现在叫 `File Browser`。

## 我想做的，其实不是“功能更多”

这个项目的目标，从一开始就不是去堆协议支持，而是想把一件事情做好：

**用一套统一的桌面交互，管理多种不同来源的文件。**

也就是说，不管你面对的是：

- 本地目录
- SFTP 服务器
- S3 Bucket
- OSS Bucket

你都尽量用同一种方式去理解、浏览、搜索、拖拽、打开、删除、传输它们。

这件事说起来简单，真正做的时候才会发现，它不是“把几个 SDK 接进来”就结束了。

因为一旦你真的想把它做成一个可以长期使用的桌面工具，问题马上就会变成：

- 路径怎么统一
- 目录树和工作区怎么联动
- 对象存储没有真正目录时怎么表现
- 拖拽在不同平台、不同 WebView 里怎么兼容
- 上传下载之后，列表、目录树、任务、通知怎么同步
- 文件操作到底要更像 IDE，还是更像 Finder / Explorer

所以这个项目越做到后面，越像是在整理一套“跨存储的文件管理体验”，而不只是写几个 API 调用。

## 它现在大概是什么样子？

从体验上看，它更接近一个偏 VS Code 风格的桌面文件管理器：

- 左边是连接和目录树
- 中间是文件工作区
- 支持多标签和分栏
- 右边有任务和通知面板
- 支持快捷键
- 支持列表视图和图标视图

从能力上看，它已经支持：

- Local / SFTP / S3 / OSS 多连接
- 跨连接拖拽传输
- 默认下载目录
- 双击打开文件
- 全局搜索和局部搜索
- 目录树和中央工作区联动
- 多语言界面
- Windows / macOS / Linux 构建

如果从“截图”看，它已经像一个完整应用了。

但我更在意的，反而是那些截图里不太容易看出来的部分。

比如：

- 空目录要不要和普通目录有完全一致的交互
- 路径到底该怎么规范化，才能兼容 Windows、Linux、macOS
- WebKit 和 WebView2 的拖拽行为到底哪里不同
- 对象存储里“目录”本来只是前缀，怎么补齐到用户能理解的语义
- 文件和目录的修改时间，能不能在上传、下载、跨连接传输时尽量保住

这些问题每一个单拿出来都不大，但它们累积起来，才决定这个工具到底是“演示可用”，还是“长期可用”。

## 为什么我觉得这个项目有意思？

因为它不是一个纯展示性质的小项目。

它涉及的东西其实挺杂：

- Go 后端
- Vue 3 前端
- Wails 桌面壳
- Local / SFTP / S3 / OSS 多驱动
- 任务系统
- 快捷键系统
- 多分栏和多标签
- WebKit / WebView2 兼容
- 路径、拖拽、时间戳、目录语义这些细节问题

也就是说，它已经不是那种“写一个页面、接一个接口”就能结束的项目。

而真正让我觉得值得写一篇文章记录它的，是另一个事实：

## 这个项目 95% 以上的代码，由 Codex、Claude 等 AI Agent 完成

这不是噱头，是这个项目到现在为止非常真实的一件事。

**本项目 95% 以上的代码，由 Codex、Claude 等 AI Agent 直接参与完成。**

包括：

- 前后端功能实现
- 拖拽与交互改造
- 路径与配置兼容处理
- 目录树与文件面板联动
- 单元测试补充
- CI / GitHub Actions 工作流
- 文档整理和 README 重写

很多人会把“AI 写代码”理解成：

- 写几个 demo
- 补几个工具函数
- 帮忙生成一点模板代码

但这个项目带来的感受不太一样。

它更像是：

- 人来决定方向和取舍
- 人来发现真实使用中的问题
- Agent 持续承担大量实现、排查、修复、重构和测试工作

换句话说，这不是“AI 帮忙写一点代码”，而是**AI Agent 已经成为这个项目的主要工程产能来源**。

当然，这并不意味着“人可以什么都不管”。

恰恰相反，Agent 真正大规模参与之后，反而更要求人去做好这些事情：

- 约束架构边界
- 审查改动是否合理
- 判断哪些问题应该局部修、哪些应该整体重构
- 补测试，防止回归
- 区分哪些能力是前端能做的，哪些必须下沉到原生层

所以我越来越觉得，真正的变化不是“AI 代替开发者”，而是：

**软件开发正在变成一种“人负责判断，Agent 负责高密度实现”的协作模式。**

而这个项目，就是这种协作模式的一个很具体的样本。

## 对普通用户来说，它有什么意义？

如果你不是开发者，也许不会特别关心“它用了什么框架”“后端是什么语言”。

但你会关心这些更实际的问题：

- 我能不能把多个文件来源放进一个工具里管理
- 我能不能少打开几个窗口、少记几套操作逻辑
- 我能不能像操作本地文件一样去处理远程文件
- 我能不能更轻松地完成上传、下载、整理、搜索、拖拽这些日常动作

如果这些问题正好是你的问题，那这个项目的价值就很直接。

它不是试图替代所有专业工具，而是想把“跨存储文件管理”这件事做得更统一、更顺手、更接近日常使用习惯。

## 对开发者来说，它还有另一层意义

这个项目还有一层我觉得同样值得关注的意义：

它说明了一件越来越现实的事。

在一个已经有一定复杂度的真实项目里，AI Agent 已经不只是“辅助”，而是可以承担绝大部分具体实现工作。

这会反过来改变很多事情：

- 需求描述会更重要
- 代码 review 会更重要
- 测试会更重要
- 架构设计会更重要
- 人类工程师的角色，会越来越偏向系统设计、判断和约束

这件事我觉得比“某个具体功能做出来了”更值得记录。

因为它不是一个功能点，而是一种工作方式的变化。

## 这个项目接下来还能往哪里走？

还有很多可以继续做的方向，比如：

- 更成熟的批量文件操作
- 更完整的搜索体验
- 更丰富的对象存储元数据支持
- 更好的文件预览与编辑能力
- 更深的多平台原生拖拽支持
- 更完整的打包、签名和发布流程

如果这些方向继续补齐，它会越来越接近一个真正适合长期使用的跨存储桌面文件管理器。

## 最后

我觉得 `File Browser` 到现在最有意思的地方，不只是它能连本地、SFTP、S3、OSS。

更重要的是，它试图把原本分散、技术感很强、体验又割裂的文件处理方式，整理成一套普通用户也能逐步适应的桌面交互。

而与此同时，它也把另一件事摆到了台前：

**在真实项目中，AI Agent 已经可以完成 95% 以上的工程实现。**

这个事实，本身就足够值得写下来。

如果你对桌面文件管理、跨存储工作流，或者 AI Agent 如何真正进入软件开发流程这三件事中的任意一件感兴趣，这个项目都值得关注。
