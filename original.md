本项目是wails 2.11 + frontent(vite + vue3 + vue router + element plus + 其他依赖)，最终目标是设计成一个支持多种远程连接接入模式（S3/OSS/SFTP/本地）的文件管理系统。关于远程连接接入的代码在folder/目录，包括S3、OSS等，前端在frontend目录，设计交互逻辑整体参照vscode。

整个工作流程如下：

后端需要的模块：

配置模块：保存、读取配置，实体entity位于config目录下，用于启动时判断日志存储路径、配置设置、远程连接目录路径等。远程连接的配置单独存放，包括例如AK、SK等跟驱动相关的配置。配置支持YAML/JSON两种格式，启动时根据文件后缀判断JSON还是YAML，默认YAML；默认配置文件路径存放于当前目录，支持启动参数传入配置位置，项目的go mod引入了github.com/spf13/viper。

日志模块：在根据配置打印路径等，决定打印的位置，默认是stdout，本地logs/目录。

目录驱动及其管理器：位于folder/目录下，有公共方法/结构，也有专有方法/结构。

文件操作模块：用于接收来自前端的文件操作回调，并且使用目录驱动进行对文件的操作。如果涉及目录之间的文件文件拖放，需要在这一层封装，而非直接在目录驱动封装。包括搜索的回调，接受前端的搜索请求，并发启动针对已连接的驱动的文件搜索，并异步返回给前端。

应用模块：用于联动调度Wails App，配置，驱动、文件管理等一系列模块。

前端：

空白页：完全没有连接的时候显示空白页，上面有一些常见的快捷键的展示，用iconify，可以参考原有的空白页frontend/src/views/Blank/FrontPage.vue

中央Tab分割面板：在骨架屏的中间，多个tab用于进行文件和菜单操作。

骨架屏幕：在frontend\src\views\Skeleton的基础上做拓展，左侧第一个菜单是目录，用mdi:folder做图标。这个是整个前端的底架，空白页放在骨架屏幕的中间（如果没有打开任何目录的话），中央位置是中央Tab分割面板，如果打开了某个目录或者设置菜单。目录tab里展示的是已经保存好的连接，这个目录树可以用element plus的el-tree-v2实现，也可以你自己实现目录树，取决于能否实现功能。目录树首层为连接，图标为Platform ，右侧有设置图标按钮，为三个点，点击下拉，菜单包括编辑、删除；双击树首层为发起连接，展开为目录树，只展示目录，不展示文件；目录图标为folder，展开时图标为folder opened。双击打开一个连接时，该连接的文件目录会展示在中央Tab分割面板内。中央Tab分割面板存在多个分隔面板时，新建的连接会在最后点击并聚焦的面板内新增。第二个骨架Tab是搜索，向后端发送搜索请求并异步接受搜索结果。左下有个设置按钮，打开整个项目的全局配置，包括日志目录等。骨架右侧菜单第一个按钮是上传任务列表，点击展开后是文件操作的历史和结果。第二个菜单是通知面板，对于目录/后端等报错可以展示在这里。

文件面板页面：点击首层连接，会打开文件面板到中央分隔面板。中央分隔面板除了空白页就是文件面板。文件面板右上侧有小图标，表示支持图标视图、列表试图。图标视图展示为大图标加标题，列表视图展示为列表，用小图标。目录使用目录图标之外，其他文件可能使用不同的图标，要求后期方便拓展图标，最初使用只使用mdi:file。要求不同后缀类型匹配不同图标。对于每个文件/目录，支持右键菜单，包括重命名、删除，要求触发后端的文件操作回调。从外部拖入文件/目录到中央文件面板页面内，为上传到目标连接；如果从一个面板拖动到另一个，下载到本地临时目录，上传到目标目录。

设置页面：通过骨架屏幕左下按钮触发打开或者快捷键打开，在中央分隔面板内作为tab的一部分。根据需要调配菜单，传到后端，保存。

新建连接页面：新建一个远程连接，选择不同种类的远程连接有不同的参数，这里要求模块化支持后期拓展。填写完成发给后端保存到远程连接的配置文件。

窗口或者程序内全局快捷键：

- CtrlOrCmd+Shift+N：新建连接
- CtrlOrCmd+.：打开设置页面。



前端所有组件需要适配主题。



先探索整个项目，再决定如何进行。

在做修改之前，你需要了解对应模块的构成和使用方式。所有的模块都可以根据需要进行修改。可以根据需要增减模块。

前端包含分割面板SplitPane、标签Tabs等基础组件，你可能需要实现其他的基础组件。

在使用wails的JS接口或者GO接口时，你需要根据godoc或者实际接口的结构来进行编码。

你需要根据整个项目的结构决定配置文件结构。

帮我理解并拆分任务，梳理配置和模块，逐个进行开发。